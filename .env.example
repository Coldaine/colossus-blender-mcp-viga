# Optional local env file (personal tooling repo)
#
# Preferred: set API keys as user-level environment variables in your OS.
# Optional: copy this file to `.env` for local convenience.
# Never commit `.env`.

# LLM provider keys (optional; set the ones you use)
ANTHROPIC_API_KEY=
GOOGLE_AI_API_KEY=
ZAI_API_KEY=

# Local model endpoint (optional)
QWEN3_VL_ENDPOINT=http://127.0.0.1:8001/v1

# Blender bridge connection (optional; depends on your bridge implementation)
BLENDER_HOST=localhost
BLENDER_PORT=9876
