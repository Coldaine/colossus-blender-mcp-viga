# Minimal dependencies for the rebuild (personal tooling repo).
# Keep this file small; add packages only when new code lands.

# LLM / API clients
anthropic>=0.39.0
requests>=2.31.0
aiohttp>=3.9.0

# Core utilities
pydantic>=2.6.0
python-dotenv>=1.0.0

# Images (optional now, but commonly needed for VLM workflows)
Pillow>=10.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# NOTE:
# - Do not depend on the `asyncio` PyPI package (Python ships asyncio).
# - Avoid OpenCV until needed; Python 3.13 wheels may not be available.
