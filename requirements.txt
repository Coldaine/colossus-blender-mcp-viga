# Core dependencies
anthropic>=0.39.0

# Z.AI GLM-4.5V Vision Integration (Legacy - kept for backward compatibility)
# For GLM-4.5V API access (https://api.z.ai/)
aiohttp>=3.9.0
requests>=2.31.0

# Async support
asyncio>=3.4.3

# Image processing
Pillow>=10.0.0
opencv-python>=4.8.0  # For hybrid VLM+OpenCV analysis in vision_evaluator
numpy>=1.24.0  # Required by OpenCV

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Local Vision Model (Qwen2.5-VL-72B)
# Deployment via SGLang or vLLM (see config/deploy_vision_model.sh)
# Note: Run deployment script to install SGLang with ROCm support
# pip install "sglang[all]" --extra-index-url https://download.pytorch.org/whl/rocm6.0

# Blender MCP server (installed via uvx)
# Run: uvx blender-mcp (this will be invoked through Claude Desktop config)

# Optional: For code generation (if using Claude)
langchain>=0.3.0
langchain-anthropic>=0.3.0

# Optional: Local development
python-dotenv>=1.0.0
pytest>=8.0.0
pytest-asyncio>=0.23.0

# JSON parsing and utilities
pydantic>=2.0.0

# Game Asset Pipeline Dependencies (Phase 2)
# These will be needed for game-ready asset export
# Uncomment when implementing game asset features:
# bpy>=3.6.0  # Blender Python API (if running standalone)
# trimesh>=4.0.0  # For mesh analysis and validation
# pygltflib>=1.16.0  # For GLTF export support
